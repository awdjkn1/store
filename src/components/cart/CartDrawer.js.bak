// CartDrawer has been deprecated: cart is now a dedicated /cart page.
// Keeping a lightweight placeholder so any stray imports won't break builds.
import React from 'react';

const CartDrawer = () => null;

export default CartDrawer;